# Start a line with a Hashtag to place a comment

# Movement Keys
bind forward w
bind backward s
bind left a
bind right d
bind jump space
bind free_look c

# Weapon Keys
bind shoot mouse1
bind w_up mouse_scroll_up
bind w_down mouse_scroll_down
bind reload r

# Utility Keys
bind console QuoteLeft
bind quit escape
bind kill f3
bind respawn f2
bind main_menu f1
bind fullscreen f11

# Musical Buttons
bind kb_1 f
bind kb_2 t
bind kb_3 g
bind kb_4 y
bind kb_5 h
bind kb_6 u
bind kb_7 j
bind kb_8 i
bind kb_9 k
bind kb_10 o
bind kb_11 l
bind kb_12 p