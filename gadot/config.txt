# Start a line with a Hashtag to place a comment

# Movement Keys
bind forward w
bind backward s
bind left a
bind right d
bind jump space
bind free_look c

# Weapon Keys
bind shoot mouse1
bind w_up mouse_scroll_up
bind w_down mouse_scroll_down
bind reload r

# Utility Keys
bind console QuoteLeft
bind quit escape
bind kill f3
bind respawn f2
bind main_menu f1
bind fullscreen f11

# Weapon Buttons
bind w1 1
bind w2 2
bind w3 3
bind w4 4
bind w5 5
bind w6 6
bind w7 7
bind w8 8
bind w9 9

# Musical Buttons
bind C f
bind CS t
bind D g
bind DS y
bind E h
bind F u
bind FS j
bind G i
bind GS k
bind A o
bind AS l
bind B p

# Settings
camera_fov = 85
viewmodel_camera_fov = 85
max_fps = 144
mouse_sensitivity = 0.1
master_volume = 0.7 # Set between 0.0 and 1.0

# Clear the console
clear
